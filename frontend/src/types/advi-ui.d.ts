// TEMP: advi-ui@0.6.0's published dist is missing dist/advi-ui.css.d.ts (and
// the other CSS type stubs) — the exports map promises them but they weren't
// built before publish. Remove this once that's fixed upstream.
declare module 'advi-ui/styles';
