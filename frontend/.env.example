# Base URL of the backend API, no trailing slash.
# Leave empty for local dev (Vite proxies /api/v1 to localhost:6754, see vite.config.ts).
# Set to your Contabo backend URL (e.g. https://api.your-domain.com) when deploying to Vercel.
VITE_API_BASE_URL=
